name: Build Paper

on:
  push:
    branches:
      - main
      # Temporary: remove the line below after merging this PR
      - copilot/setup-research-paper-with-showyourwork
    paths:
      - 'src/tex/**'
      - 'src/scripts/**'
      - 'showyourwork.yml'
      - 'environment.yml'
      - '.github/workflows/build-paper.yml'
  pull_request:
    paths:
      - 'src/tex/**'
      - 'src/scripts/**'
      - 'showyourwork.yml'
      - 'environment.yml'
      - '.github/workflows/build-paper.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build research paper
    permissions:
      contents: read
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          
      - name: Install showyourwork
        run: |
          pip install showyourwork "pulp<2.8.0"
          
      - name: Build paper
        run: |
          showyourwork build
          
      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: paper-pdf
          path: ms.pdf
          
      - name: Upload build report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-report
          path: report.html
